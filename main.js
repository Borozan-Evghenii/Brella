// Tabs for services section
const servicesButtons = document.querySelectorAll(".services-list__item");
const servicesContent = document.querySelectorAll(
  ".services-description__content"
);

servicesButtons.addEventListener("click", (element) => {
  console.log(element);
});
